
# 源文件包报告证据升级记录

未修改官方模型、正式控制器逻辑或64份冻结raw。

## 完成内容

- 读者可见主算法名称统一为`RA-GCA-CGHTE`，正式固定参数和诊断码914写入campaign合同并纳入测试。
- `RUN_ALL.ps1`与Syslab配对评价入口强制使用包外绝对输出目录。
- 新增`04_results/report_evidence`共14份结构化证据，包含五对象覆盖、三级演进、11项参数配对、32项边界、外扰取舍、复合应力代价、编队安全和结论证据索引。
- 新增10张`1600 x 900`报告候选静态图；原3张发布静态图和6个单主题GIF保持独立登记。
- Scene07B的队形误差RMSE与三机全局位置跟踪RMSE已分列，禁止混写。
- 三事件外扰中CP-INDI的局部优势和复合应力恢复时间代价均被如实保留。
- 三个依赖旧工作区且无法在当前包内闭合的开发脚本已非破坏性迁出到包外回滚区，不再作为交付入口。

## 复算与验收

- `RECOMPUTE_REPORT_EVIDENCE.ps1 -Check`只读校验证据。
- `RECOMPUTE_REPORT_EVIDENCE.ps1 -OutputDirectory <包外路径>`确定性重建证据。
- `generate_report_figures.py --check`只读校验候选图；包内重建必须显式使用维护者参数。
- `RUN_QUICK_VERIFY.ps1`统一执行主算法、报告证据、图件、入口、Syslab和整包完整性验收。

## 声明边界

32项是项目回归集，不是官方规定测试数量。32/32逐项配对门和物理门通过，但`required_parameter_retention=false`保持不变；V30、H60、Wilson下界和配对bootstrap尚未完成。10张候选图尚未分配最终报告图号。
